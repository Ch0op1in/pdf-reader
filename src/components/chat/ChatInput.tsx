

interface ChatInputProps{
    isDisabled?: boolean
}

const ChatWrapper = ({isDisabled} : ChatInputProps) => {
    return(
        <div>
            
        </div>
    )
}

export default ChatWrapper